/* icons.tsx — single import surface for app icons.
 *
 * One icon set (lucide-react), one stroke width, one visual language. Add new
 * glyphs here as features need them; never import lucide-react directly from a
 * component file.
 */
export {
  Plus,
  Minus,
  Search,
  ShoppingBag,
  User,
  Menu,
  Sun,
  Moon,
  X,
} from 'lucide-react'
